for (let i = 1; i <= 10; i++) {
    const parImpar = i % 2 === 0 ? 'par' : 'impar'
    console.log(i, parImpar)
}

const frutas = ['Maçã', 'Manga', 'Uva', 'Melão', 'Banana']

for (i = 0; i < frutas.length; i++) {
    console.log(`Índice ${i} (${frutas[i]})`)
}
