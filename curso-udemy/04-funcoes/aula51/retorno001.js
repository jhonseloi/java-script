// return = retorna um valor e encerra a função

function soma(a, b) {
    return a + b // Retorna valor
}

console.log(soma(2, 5))

function soma2(a, b) {
    console.log(a + b) // NÃO retorna valor, apenas mostra a soma dos valores
}

soma2(2, 5)
