// For of -> Retorna o valor em si (objetos iteráveis: strings ou arrays)

const pessoas = ['Marcelo', 'Henrique', 'Diego']
for (let valor of pessoas) {
    console.log(valor)
}

const pessoa = 'Luiz'
for (let valor of pessoa) {
    console.log(valor)
}
