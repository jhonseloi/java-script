function saudacao(nome) { // Função e parâmetro nomeados
    return `Bom dia, ${nome}!` // Return encerra a execução da função
}

const variavel = saudacao('João') // É atribuído um valor (argumento) ao parâmetro, que é chamado dentro da função
console.log(variavel)
